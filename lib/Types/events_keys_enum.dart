
enum EStores{
  data,
  navigation
}

enum EEventDataKeys{
  instance,
  instanceCollection,
  operation,
}

enum EEvents{
  add,
  edit,
  remove,
  load,
  search
}

enum EOperationStatus{
  success,
  error,
}

enum EOperations{
  printPurchase,
}